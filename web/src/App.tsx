// Placeholder shell for the vllm-calc SPA (Story 1.1). The real single-screen
// calculator — inputs, VerdictBanner, VramBreakdownBar — arrives in Stories
// 1.9–1.11 per the UX design specification.

export default function App() {
  return (
    <main>
      <h1>vllm-calc</h1>
      <p>Will your model fit on your GPUs? (Calculator coming in Stories 1.9–1.11.)</p>
    </main>
  )
}
